machine:
  services:
    - docker
  node:
    version: 4.2.3
dependencies:
  override:
    - docker pull mongo:latest
test:
  override:
    - npm install
    - npm test
general:
  artifacts:
    - coverage
