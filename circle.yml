machine:
  services:
    - docker
  node:
    version: 6.2.0
dependencies:
  cache_directories:
    - node_modules
test:
  override:
    - npm install
    - npm test
general:
  artifacts:
    - coverage
