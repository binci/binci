from: mhart/alpine-node:6
services:
  - mongodb:
      from: mongo:3.0
tasks:
  env: env
  clean: rm -rf node_modules
  install: npm install
  test: npm run test
  lint: npm run lint
  shell: /bin/sh
